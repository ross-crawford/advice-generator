const Header = () => (
  <header className="header">
    <span className="header-title">Discimus</span>
    <span className="header-subtitle">Advice Generator</span>
  </header>
);

export default Header;
